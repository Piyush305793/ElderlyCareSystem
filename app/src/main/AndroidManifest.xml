<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.elderlycaresystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElderlyCareSystem">
        <activity android:name=".Profile"></activity>
        <activity android:name=".Diseases" />
        <activity android:name=".PreventInfection" />
        <activity android:name=".Yoga_Exercise" />
        <activity android:name=".Balanced_Diet" />
        <activity android:name=".Basic_Tips" />
        <activity android:name=".Dashboard" />
        <activity android:name=".Splash_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>